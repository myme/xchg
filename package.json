{
  "name": "xchg",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently -k -c green,blue npm:start:*",
    "start:frontend": "cd frontend && yarn start",
    "start:backend": "cd server && yarn start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^3.6.1"
  },
  "workspaces": [
    "frontent",
    "server"
  ]
}
